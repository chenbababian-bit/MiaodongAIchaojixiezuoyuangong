# 营销调研模块开发文档

## 📋 概述

本文档记录了在营销模块下添加"调研"分类的完整开发过程，包含10个子分类、共74个专业模板的完整层级结构。

**开发时间**: 2026-02-11
**参考文档**: docs/营销媒介模块开发文档.md

---

## 🎯 需求分析

### 原始需求
- 在营销模块下添加"调研"分类
- 创建10个子分类，总计74个专业模板
- 遵循与媒介模块相同的UI/UX模式
- 不显示用户数量

### 目标结构
```
营销 (Marketing)
└── 调研 (Research)
    ├── 调研策略 (7个模板)
    ├── 设计执行 (7个模板)
    ├── 数据分析 (8个模板)
    ├── 市场分析 (14个模板)
    ├── 调研结果 (7个模板)
    ├── 效果评估 (8个模板)
    ├── 项目管理 (7个模板)
    ├── 培训教育 (6个模板)
    ├── 法律合规 (5个模板)
    └── 技术工具 (5个模板)
```

---

## 📁 文件结构

### 主要涉及文件
1. **lib/marketing-templates.ts** - 模板数据定义文件
2. **components/marketing-page.tsx** - 营销页面组件

---

## 🔢 ID分配策略

### ID范围规划

为避免ID冲突，采用以下分配策略：

| 子分类 | ID范围 | 起始ID | 结束ID | 模板数量 |
|--------|--------|--------|--------|----------|
| 调研策略 | 15001-15099 | 15001 | 15007 | 7 |
| 设计执行 | 15101-15199 | 15101 | 15107 | 7 |
| 数据分析 | 15201-15299 | 15201 | 15208 | 8 |
| 市场分析 | 15301-15399 | 15301 | 15314 | 14 |
| 调研结果 | 15401-15499 | 15401 | 15407 | 7 |
| 效果评估 | 15501-15599 | 15501 | 15508 | 8 |
| 项目管理 | 15601-15699 | 15601 | 15607 | 7 |
| 培训教育 | 15701-15799 | 15701 | 15706 | 6 |
| 法律合规 | 15801-15899 | 15801 | 15805 | 5 |
| 技术工具 | 15901-15999 | 15901 | 15905 | 5 |

**规则说明**：
- 每个子分类预留100个ID空间（xx01-xx99）
- 便于后续扩展新模板
- ID连续性保证数据管理的清晰性

---

## 🛠️ 实施步骤

### 步骤1：定义子分类结构

在 `lib/marketing-templates.ts` 中添加子分类数组：

```typescript
export const researchSubCategories = [
  { id: "research-strategy", label: "调研策略" },
  { id: "design-execution", label: "设计执行" },
  { id: "data-analysis", label: "数据分析" },
  { id: "market-analysis-research", label: "市场分析" },
  { id: "research-results", label: "调研结果" },
  { id: "effect-evaluation", label: "效果评估" },
  { id: "project-management-research", label: "项目管理" },
  { id: "training-education", label: "培训教育" },
  { id: "legal-compliance-research", label: "法律合规" },
  { id: "technical-tools", label: "技术工具" },
];
```

### 步骤2：创建模板数组

为每个子分类创建对应的模板数组。以"调研策略"为例：

```typescript
export const researchStrategyTemplates = [
  {
    id: 15001,
    icon: "📅",
    title: "市场调研年度计划",
    desc: "制定一份详细的市场调研年度计划，为企业提供全面的市场信息和决策依据。",
    color: "bg-blue-500",
  },
  // ... 更多模板
];
```

**模板数据结构**：
- `id`: 唯一标识符（number类型）
- `icon`: Emoji图标（string类型）
- `title`: 模板标题（string类型）
- `desc`: 模板描述（string类型）
- `color`: Tailwind CSS颜色类（string类型）

### 步骤3：更新组件导入

在 `components/marketing-page.tsx` 中导入所有新模板数组：

```typescript
import {
  // ... 其他导入
  researchSubCategories,
  researchStrategyTemplates,
  designExecutionTemplates,
  dataAnalysisTemplates,
  marketAnalysisResearchTemplates,
  researchResultsTemplates,
  effectEvaluationTemplates,
  projectManagementResearchTemplates,
  trainingEducationTemplates,
  legalComplianceResearchTemplates,
  technicalToolsResearchTemplates,
} from "@/lib/marketing-templates";
```

### 步骤4：配置调研分类

在 `marketing-page.tsx` 组件中配置 `researchCategories`：

```typescript
const researchCategories = [
  { id: "research-strategy", label: "调研策略", templates: researchStrategyTemplates },
  { id: "design-execution", label: "设计执行", templates: designExecutionTemplates },
  { id: "data-analysis", label: "数据分析", templates: dataAnalysisTemplates },
  { id: "market-analysis-research", label: "市场分析", templates: marketAnalysisResearchTemplates },
  { id: "research-results", label: "调研结果", templates: researchResultsTemplates },
  { id: "effect-evaluation", label: "效果评估", templates: effectEvaluationTemplates },
  { id: "project-management-research", label: "项目管理", templates: projectManagementResearchTemplates },
  { id: "training-education", label: "培训教育", templates: trainingEducationTemplates },
  { id: "legal-compliance-research", label: "法律合规", templates: legalComplianceResearchTemplates },
  { id: "technical-tools", label: "技术工具", templates: technicalToolsResearchTemplates },
];
```

### 步骤5：添加路由逻辑

在 `handleTemplateClick` 函数中添加调研路由处理：

```typescript
const handleTemplateClick = (templateId: number, templateTitle: string) => {
  let source = "";
  // ... 其他分类处理

  // 调研分类路由
  if (activeSecondLevel === "research") {
    source = `marketing-research-${activeThirdLevel}`;
  }

  // 构建完整路径
  router.push(`/writing/xiaohongshu?template=${templateId}&title=${encodeURIComponent(templateTitle)}&source=${source}`);
};
```

### 步骤6：实现三级导航显示

在渲染逻辑中添加调研子分类导航：

```typescript
{activeSecondLevel === "research" && (
  <div className="space-y-1">
    {researchSubCategories.map((category) => (
      <button
        key={category.id}
        onClick={() => scrollToCategory(category.id)}
        className={cn(
          "w-full text-left px-4 py-3 rounded-lg text-sm font-medium transition-colors mb-1",
          activeThirdLevel === category.id
            ? "bg-primary text-primary-foreground"
            : "hover:bg-muted text-foreground"
        )}
      >
        {category.label}
      </button>
    ))}
  </div>
)}
```

### 步骤7：添加主内容区域渲染

```typescript
{activeSecondLevel === "research" && (
  <div className="space-y-8">
    {researchCategories.map((category) => (
      <div key={category.id} id={`category-${category.id}`}>
        <h2 className="text-xl font-semibold mb-4">{category.label}</h2>
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4">
          {category.templates.map((template) => (
            <Card
              key={template.id}
              className="p-4 cursor-pointer hover:shadow-lg transition-shadow"
              onClick={() => handleTemplateClick(template.id, template.title)}
            >
              {/* 模板卡片内容 */}
            </Card>
          ))}
        </div>
      </div>
    ))}
  </div>
)}
```

---

## ⚠️ 常见问题与解决方案

### 问题1：模板数组名称冲突

**错误信息**：
```
the name `technicalToolsTemplates` is defined multiple times
```

**原因**：
创意分类中已经有一个 `technicalToolsTemplates`，调研分类中又创建了同名的。

**解决方案**：
将调研分类中的技术工具模板重命名为 `technicalToolsResearchTemplates`：

```typescript
// 修改前
export const technicalToolsTemplates = [...]

// 修改后
export const technicalToolsResearchTemplates = [...]
```

---

## ✅ 测试验证清单

### 功能测试
- [x] 三级导航正常显示
- [x] 点击子分类按钮能正确切换
- [x] 模板卡片正确渲染（图标、标题、描述）
- [x] 点击模板能正确跳转到详情页
- [x] 路由参数正确传递

### 数据完整性
- [x] 所有10个子分类都已添加
- [x] 总计74个模板全部存在
- [x] ID无重复、无遗漏
- [x] 每个模板数据结构完整

### UI/UX一致性
- [x] 样式与媒介模块保持一致
- [x] 响应式布局正常工作
- [x] 颜色主题协调统一
- [x] 无用户数量显示

### 构建测试
- [x] `npm run build` 成功执行
- [x] 无TypeScript类型错误
- [x] 无命名冲突错误

---

## 📝 代码规范

### 命名约定
- 子分类ID：使用kebab-case（如 `research-strategy`）
- 模板数组：使用camelCase + Templates后缀（如 `researchStrategyTemplates`）
- 组件状态：使用camelCase（如 `activeSecondLevel`）

### 数据结构规范
```typescript
// 子分类定义
interface SubCategory {
  id: string;        // kebab-case
  label: string;     // 中文显示名称
}

// 模板定义
interface Template {
  id: number;        // 唯一数字ID
  icon: string;      // Emoji字符
  title: string;     // 模板标题
  desc: string;      // 模板描述
  color: string;     // Tailwind颜色类
}

// 分类配置
interface CategoryConfig {
  id: string;           // 与SubCategory.id对应
  label: string;        // 与SubCategory.label对应
  templates: Template[]; // 该分类下的所有模板
}
```

---

## 🔄 扩展指南

### 添加新子分类

1. **在 `marketing-templates.ts` 中**：
   ```typescript
   // 1. 添加到 researchSubCategories
   export const researchSubCategories = [
     // ... 现有分类
     { id: "new-category", label: "新分类" },
   ];

   // 2. 创建模板数组（ID范围：16001-16099）
   export const newCategoryTemplates: Template[] = [
     {
       id: 16001,
       icon: "🆕",
       title: "新模板",
       desc: "新模板描述",
       color: "bg-purple-500"
     },
   ];
   ```

2. **在 `marketing-page.tsx` 中**：
   ```typescript
   // 1. 导入新模板数组
   import { newCategoryTemplates } from "@/lib/marketing-templates";

   // 2. 添加到 researchCategories
   const researchCategories = [
     // ... 现有分类
     { id: "new-category", label: "新分类", templates: newCategoryTemplates },
   ];
   ```

### 添加新模板到现有子分类

1. 确认目标子分类的ID范围
2. 选择下一个可用ID
3. 在对应的模板数组中添加新模板对象
4. 确保数据结构完整

**示例**：向"调研策略"添加第8个模板
```typescript
export const researchStrategyTemplates = [
  // ... 现有7个模板（15001-15007）
  {
    id: 15008,  // 下一个可用ID
    icon: "🎨",
    title: "创意调研策略",
    desc: "制定创意调研策略",
    color: "bg-pink-500"
  },
];
```

---

## 📊 项目统计

- **总子分类数**: 10个
- **总模板数**: 74个
- **ID范围**: 15001-15905
- **代码行数**: 约800行（仅模板数据）
- **开发时间**: 约1小时
- **文件修改**: 2个主要文件

---

## 🎓 经验总结

### 成功要素
1. **清晰的ID分配策略**：预留足够空间，避免后续冲突
2. **模块化数据结构**：每个子分类独立数组，便于维护
3. **一致的命名规范**：提高代码可读性
4. **避免命名冲突**：使用明确的后缀区分不同模块的同名数组

### 改进建议
1. 考虑将模板数据迁移到数据库或JSON文件
2. 实现模板搜索和筛选功能
3. 添加模板收藏和历史记录功能
4. 优化大量模板的渲染性能（虚拟滚动）

---

## 📚 相关资源

- **参考文档**: [docs/营销媒介模块开发文档.md](./营销媒介模块开发文档.md)
- **主要文件**:
  - [lib/marketing-templates.ts](../lib/marketing-templates.ts)
  - [components/marketing-page.tsx](../components/marketing-page.tsx)

---

## 📞 维护联系

如遇到问题或需要扩展功能，请参考本文档的"扩展指南"部分，或查看相关代码文件中的注释。

**文档版本**: 1.0
**最后更新**: 2026-02-11
