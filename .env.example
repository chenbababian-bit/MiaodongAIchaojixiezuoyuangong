# ============================================
# 环境变量配置文件
# ============================================
# 复制此文件为 .env.local 并填写实际的配置值
# 注意：.env.local 不会被提交到 Git 仓库

# ============================================
# DeepSeek API 配置（必需）
# ============================================
# 从 https://platform.deepseek.com 获取 API Key
DEEPSEEK_API_KEY=your-api-key-here

# DeepSeek API 地址（可选）
# 默认值：https://api.deepseek.com/v1/chat/completions
# 如果使用自定义端点，请取消注释并修改
# DEEPSEEK_API_URL=https://api.deepseek.com/v1/chat/completions

# ============================================
# 历史记录存储配置（可选）
# ============================================
# 控制历史记录的存储方式
# - false 或不设置：使用浏览器 localStorage（本地测试推荐）
# - true：使用数据库存储（生产环境推荐，需配置 Supabase）
NEXT_PUBLIC_USE_DATABASE=false

# ============================================
# Supabase 配置（使用数据库存储时必需）
# ============================================
# 从 https://supabase.com 获取项目配置
# 项目设置 -> API -> Project URL
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url

# 项目设置 -> API -> Project API keys -> anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# 项目设置 -> API -> Project API keys -> service_role (仅服务端使用)
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# ============================================
# Vercel 部署说明
# ============================================
# 在 Vercel Dashboard 中配置以下环境变量：
# 1. DEEPSEEK_API_KEY（必需）
# 2. DEEPSEEK_API_URL（可选）
# 3. NEXT_PUBLIC_USE_DATABASE（可选，默认 false）
# 4. NEXT_PUBLIC_SUPABASE_URL（使用数据库时必需）
# 5. NEXT_PUBLIC_SUPABASE_ANON_KEY（使用数据库时必需）
# 6. SUPABASE_SERVICE_ROLE_KEY（使用数据库时必需）
#
# 详细部署指南请查看：VERCEL_DEPLOYMENT.md

