# Vercel 部署指南

## 📦 项目信息
- **项目名称**: 秒懂AI超级员工 - 专业AI写作助手
- **框架**: Next.js 16.0.10
- **Node 版本**: 建议 18.x 或更高

## 🔑 必需的环境变量

在 Vercel 项目设置中配置以下环境变量：

### 1. DeepSeek API 密钥（必需）
```
DEEPSEEK_API_KEY=your-deepseek-api-key-here
```

### 2. DeepSeek API 地址（可选）
```
DEEPSEEK_API_URL=https://api.deepseek.com/v1/chat/completions
```
> 如果不设置，将使用默认值

### 3. 数据库配置（可选）
```
NEXT_PUBLIC_USE_DATABASE=false
```
> - `false`: 使用浏览器 localStorage 存储历史记录（推荐用于初期）
> - `true`: 使用数据库存储（需要实现 /api/history 接口）

## 🚀 部署步骤

### 方式一：通过 Vercel Dashboard（推荐）

1. **登录 Vercel**
   - 访问 https://vercel.com
   - 使用 GitHub/GitLab/Bitbucket 账号登录

2. **导入项目**
   - 点击 "Add New..." → "Project"
   - 选择您的 Git 仓库
   - 点击 "Import"

3. **配置项目**
   - **Framework Preset**: Next.js（自动检测）
   - **Root Directory**: `./`（默认）
   - **Build Command**: `npm run build`（默认）
   - **Output Directory**: `.next`（默认）

4. **添加环境变量**
   - 在 "Environment Variables" 部分添加：
     ```
     DEEPSEEK_API_KEY=your-actual-api-key
     ```
   - 可选添加其他环境变量

5. **部署**
   - 点击 "Deploy" 按钮
   - 等待构建完成（约 2-3 分钟）

### 方式二：通过 Vercel CLI

```bash
# 1. 安装 Vercel CLI
npm i -g vercel

# 2. 登录
vercel login

# 3. 部署
vercel

# 4. 生产环境部署
vercel --prod
```

## ⚙️ 已完成的优化

✅ **TypeScript 配置优化**
- 移除了 `ignoreBuildErrors`，确保类型安全

✅ **图片优化**
- 启用 Vercel 图片优化，提升性能
- 配置了远程图片域名支持

✅ **动态渲染配置**
- 配置了 `dynamic = 'force-dynamic'`
- 解决了 useSearchParams 的 Suspense 问题

✅ **构建验证**
- 本地构建成功
- 所有路由正常生成

## 📊 API 路由

项目包含以下 API 端点：

- `/api/xiaohongshu` - 小红书文案生成
- `/api/video` - 短视频文案生成
- `/api/wechat-article` - 公众号文章生成

所有 API 都配置了：
- 60 秒超时限制
- 错误处理
- API Key 验证

## 🔍 部署后验证

部署成功后，请验证：

1. ✅ 首页正常加载
2. ✅ 各个写作模板页面可访问
3. ✅ API 调用正常（需要配置正确的 DEEPSEEK_API_KEY）
4. ✅ 历史记录功能正常

## 🐛 常见问题

### 1. API 调用失败
**原因**: 环境变量未配置或配置错误
**解决**:
- 检查 Vercel Dashboard → Settings → Environment Variables
- 确保 `DEEPSEEK_API_KEY` 已正确设置
- 重新部署项目

### 2. 构建失败
**原因**: 依赖安装问题
**解决**:
- 检查 Node.js 版本（建议 18.x+）
- 清除缓存重新部署

### 3. 页面加载慢
**原因**: 冷启动或 API 响应慢
**解决**:
- Vercel 免费版有冷启动延迟
- 考虑升级到 Pro 版本

## 📝 环境变量配置示例

在 Vercel Dashboard 中添加：

| 变量名 | 值 | 环境 |
|--------|-----|------|
| DEEPSEEK_API_KEY | sk-xxx... | Production, Preview, Development |
| DEEPSEEK_API_URL | https://api.deepseek.com/v1/chat/completions | Production, Preview, Development |
| NEXT_PUBLIC_USE_DATABASE | false | Production, Preview, Development |

## 🎉 部署完成

部署成功后，您将获得：
- 生产环境 URL: `https://your-project.vercel.app`
- 自动 HTTPS
- 全球 CDN 加速
- 自动预览部署（每次 Git push）

## 📞 技术支持

如遇问题，请检查：
1. Vercel 部署日志
2. 浏览器控制台错误
3. API 响应状态

---

**最后更新**: 2026-01-26
**Next.js 版本**: 16.0.10
**Node.js 要求**: >=18.0.0
